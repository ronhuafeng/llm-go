interface:
  display_name: "Codex SDK Upstream Sync"
  short_description: "Sync codexsdk with an upstream Codex tag, ref, or commit"
  default_prompt: "Use $codexsdk-sync-upstream to sync the codexsdk module with an upstream Codex tag, ref, or commit."
