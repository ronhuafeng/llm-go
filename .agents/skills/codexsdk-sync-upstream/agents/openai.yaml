interface:
  display_name: "Codex SDK Protocol Drift"
  short_description: "Implement already-observed Codex App Server protocol drift"
  default_prompt: "Use $codexsdk-sync-upstream for one targeted codexsdk implementation pass using the protocol drift already supplied by the caller. Do not resolve a different upstream target or publish changes."
